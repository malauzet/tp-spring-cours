# Development profile.
# Activate with: -Dspring-boot.run.profiles=dev
#            or: --spring.profiles.active=dev
#            or: SPRING_PROFILES_ACTIVE=dev

# Let Hibernate align the schema with the entities while developing.
spring.jpa.hibernate.ddl-auto=update

# Log every SQL statement.
spring.jpa.show-sql=true
